{
    "name": "ffmpeg-cli-online",
    "version": "1.0.0",
    "description": "A client side tool for running ffmpeg.",
    "dependencies": {
        "@ffmpeg/core": "^0.10.0",
        "@ffmpeg/ffmpeg": "^0.10.1",
        "bulma": "^0.9.4",
        "preact": "^10.7.2"
    },
    "devDependencies": {
        "esbuild": "^0.14.39",
        "serve-handler": "^6.1.3"
    },
    "scripts": {
        "build": "node esbuild.js",
        "start": "node esbuild-dev.js"
    }
}
